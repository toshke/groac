name: Go build
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Git checkout
        uses: actions/checkout@v2
      - run: ls -la
      - run: make build
      - run: ls -la build/